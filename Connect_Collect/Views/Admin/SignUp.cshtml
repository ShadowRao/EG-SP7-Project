@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model Connect_Collect.Models.AdminViewModel
<link rel="stylesheet" href="~/css/CommonStyle.css" />

@{
    ViewData["Title"] = "Admin SignUp";
}


<link rel="stylesheet" href="~/css/CommonStyle.css"/>

<h1> Admin Signup </h1>

<form method="post" id="adminSignup">
    <div class="mt-3">
        <label class="form-label"> AdminName</label>
        <input type="text" class="form-control" asp-for="AdminName"/>
    </div>
    <div class="mt-3">
        <label class="form-label"> Email</label>
        <input type="text" class="form-control" asp-for="Email"/>   
    </div>
    <div class="mt-3">
        <label class="form-label"> Password</label>
        <input type="password" id="password" class="form-control" asp-for="Password"/>
    </div>
    <div class="mt-3">
        <label class="form-label">Re-enter Password</label>
        <input type="password" class="form-control" id="repassword" required />
        <div id="passwordError" class="text-danger" style="display:none;">Passwords do not match!</div>
    </div>
    <div class="mt-3">
        <button type="submit" class="btn btn-primary" style="padding: 4px 8px; font-size: 14px;">SUBMIT</button>
    </div>

</form>

<script>
    const form = document.getElementById('adminSignup');
    const passwordInput = document.getElementById('password');
    const repasswordInput = document.getElementById('repassword');
    const passwordError = document.getElementById('passwordError');

    form.addEventListener('submit', function (event) {
        if (passwordInput.value !== repasswordInput.value) {
            event.preventDefault(); // Prevent form submission
            passwordError.style.display = 'block'; // Show error message
        } else {
            passwordError.style.display = 'none'; // Hide error message
        }
    });
</script>